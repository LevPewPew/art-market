<%= form_with(model: [@listing, @listing.comments.build], local: true) do |form| %>
  <% if @listing.comments.build.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@listing.comments.build.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @listing.comments.build.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <%= form.hidden_field(:listing_id, value: @listing.id) %>
  <%= form.hidden_field(:user_id, value: current_user.id) %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
